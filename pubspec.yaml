name: simplest_service_locator
description: Simple service locator package developed in Dart, tinier alternative to get_it
version: 2.0.2
repository: https://github.com/sunderee/simplest-service-locator

environment:
  sdk: ">=3.9.2 <4.0.0"

topics:
  - service-locator
  - simple-service-locator
  - lightweight-service-locator
  - design-pattern

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
