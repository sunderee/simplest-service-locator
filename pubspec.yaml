name: simplest_service_locator
description: Simple service locator package developed in Dart, tinier alternative to get_it
version: 2.0.0
repository: https://gitlab.com/dart-and-flutter-packages/simplest_service_locator

environment:
  sdk: ">=3.7.0 <4.0.0"

topics:
  - service-locator
  - simple-service-locator
  - lightweight-service-locator
  - design-pattern

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
